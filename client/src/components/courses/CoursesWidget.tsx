import React from 'react';

const CoursesWidget = () => {
    return (
        <div
            className="container"
        >
            CoursesWidget
            {/* component that is on the home page */}

            {/* Courses title + show all button */}
            {/* 5 courses */}
        </div>
    );
};

export default CoursesWidget
