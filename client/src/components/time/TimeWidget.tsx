import React from 'react';

const TimeWidget = () => {
    return (
        <div>
            TimeWidget
            {/* component that is in the menu */}
            {/* display time */}
            
            {/* stop, start counting */}
        </div>
    );
};

export default TimeWidget;
